package com.simple.digisave.ui.theme

import androidx.compose.ui.graphics.Color

// ── Backgrounds ───────────────────────────────────────────────
val BackgroundSoft = Color(0xFFFFF9F0)  // warm cream
val SurfaceWhite   = Color(0xFFFFFFFF)

// ── Text ──────────────────────────────────────────────────────
val TextDark = Color(0xFF2E2E2E)
val TextGray = Color(0xFF9E9E9E)

// ── Brand Palette ─────────────────────────────────────────────
val PastelGreen  = Color(0xFFA8E6CF)   // Primary  — nav bar, bubbles
val PastelBlue   = Color(0xFFA0CED9)   // Secondary
val PastelPurple = Color(0xFFBA68C8)   // Tertiary
val PastelRed    = Color(0xFFFFAAA5)   // Error
val PastelYellow = Color(0xFFFFD3B6)   // Accent / tertiary tint

// ── Semantic ──────────────────────────────────────────────────
val AccentTeal = Color(0xFF26A69A)     // Income amounts, main FAB, dark-mode primary
val ActiveBlue = Color(0xFF64B5F6)     // Active nav icon inner bubble

// ── Transaction amounts ───────────────────────────────────────
val IncomeText  = Color(0xFF2E7D32)    // Dark green — readable income text
val ExpenseText = Color(0xFFC62828)    // Dark red   — readable expense text

// ── Speed Dial FAB ────────────────────────────────────────────
val IncomeGreen = Color(0xFF7ED9A7)    // Income mini FAB
val ExpenseRed  = Color(0xFFEF8F88)    // Expense mini FAB
val IncomeChip  = Color(0xFFDFF7E6)    // Income label chip background
val ExpenseChip = Color(0xFFFFE7E2)    // Expense label chip background
